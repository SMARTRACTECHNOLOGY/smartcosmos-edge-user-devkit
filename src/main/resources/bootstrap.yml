project:
  artifactId: smartcosmos-generic-service
spring:
  application:
    name: smartcosmos-edge-user
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      failFast: true
